"""SharpEdge Orchestra: minimal kernel + Orchestra Agent vertical slice.

Proves the control loop end-to-end: Intent -> decompose -> dispatch to an
adapter -> observe -> Result, with durable, resumable, contract-validated state.
"""
